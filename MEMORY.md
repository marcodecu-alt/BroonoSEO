# Broono SEO Pipeline — project memory

Running log of cross-cutting learnings, architectural decisions, and user feedback about this
project as a whole. Agent-specific learnings belong in that agent's own MEMORY.md under
`apps/api/app/graph/agents/<name>/` instead.

Append dated entries, do not rewrite history.

## Format

- YYYY-MM-DD — what changed/was learned — why

## Log

(empty, nothing logged yet)
